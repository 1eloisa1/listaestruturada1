/*.Coordenadas de um ponto: leia dois valores reais x e y e determine a localização
do ponto no plano cartesiano: quadrante, eixo ou origem.*/

//Coordenadas de um ponto: leia dois valores reais x e y e determine a localização do ponto no plano cartesiano: quadrante, eixo ou origem.

#include <stdio.h>
#include <math.h>

int main() {
    float x, y, quadrante, origem, eixo;

    origem = 0;

    printf("Digite o x: ");
    scanf("%f", &x); 

    printf("Digite o y: ");
    scanf("%f", &y); 

    volume = (4.0/3) * pi * pow(raio, 3);

    printf("VOLUME = %f", volume);

    return 0;
}
